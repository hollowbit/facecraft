task runMinecraftServer(type: Exec) {
    workingDir 'minecraft-server'

    //on windows
    commandLine 'cmd', '/c', 'run.bat'

    //on linux/mac
    commandLine './run.sh'

    standardInput = System.in
}